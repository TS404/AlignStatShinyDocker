# AlignStatShinyDocker
```
    docker build -t ts404:alignstatshiny_v3 github.com/TS404/AlignStatShinyDocker   # To build the container
    docker run   -p 3838:3838 ts404:alignstatshiny_v3                               # To run the container
```
